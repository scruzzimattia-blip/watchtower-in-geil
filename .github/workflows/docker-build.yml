name: Docker Build & Push to GHCR

# Ausfuehren bei jedem Push auf den main-Branch, den develop-Branch oder bei neuen Tags.
on:
  push:
    branches:
      - main
      - develop
    tags:
      - 'v*'

# Berechtigungen fuer GitHub Packages (GHCR).
permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Read version file
        id: version
        run: echo "version=$(cat VERSION)" >> $GITHUB_OUTPUT

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            # 'latest' Tag nur fuer den main Branch.
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            
            # 'develop' Tag nur fuer den develop Branch.
            type=raw,value=develop,enable=${{ github.ref == 'refs/heads/develop' }}
            
            # Die Version aus der VERSION-Datei.
            type=raw,value=${{ steps.version.outputs.version }}
            
            # SemVer Tags (fuer Git-Tags wie v1.0.0).
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            
            # SHA-Tag fuer jeden Build.
            type=sha,format=long

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
